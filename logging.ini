[formatters]
keys=default

[formatter_default]
format=%(asctime)s [%(levelname)s] (%(name)s) %(message)s
datefmt=%Y-%m-%d %H:%M:%S
class=logging.Formatter

[handlers]
keys=log_file

[handler_log_file]
class=logging.handlers.RotatingFileHandler
level=DEBUG
formatter=default
args=('cumulonimbi.log', 'a', 1024 * 1024 * 100, 10)

[loggers]
keys=root

[logger_root]
level=DEBUG
handlers=log_file
